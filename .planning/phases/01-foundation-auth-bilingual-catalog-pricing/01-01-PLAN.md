---
phase: 01-foundation-auth-bilingual-catalog-pricing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - .gitignore
  - .env.example
  - .npmrc
  - README.md
  - docker-compose.yml
  - docker-compose.dev.yml
  - apps/api/package.json
  - apps/api/nest-cli.json
  - apps/api/tsconfig.json
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/src/proxy.ts
  - apps/web/src/i18n/routing.ts
  - apps/web/src/i18n/request.ts
  - apps/web/src/i18n/navigation.ts
  - apps/web/src/app/[locale]/layout.tsx
  - apps/web/src/app/[locale]/page.tsx
  - apps/web/messages/vi.json
  - apps/web/messages/en.json
  - packages/shared-types/package.json
  - packages/shared-types/tsconfig.json
  - packages/shared-types/src/index.ts
autonomous: false
requirements: [CATALOG-09]

must_haves:
  truths:
    - "A developer can run `pnpm install && pnpm turbo run typecheck` at the repo root and both apps typecheck clean"
    - "A developer can run `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d` and get healthy postgres, redis and minio containers"
    - "Visiting http://localhost:3000/vi renders Vietnamese copy and http://localhost:3000/en renders English copy from message catalogs"
    - "No secret value exists anywhere in git-tracked files; .env.example documents every required key with a placeholder"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "pnpm workspace definition covering apps/* and packages/*"
      contains: "apps/*"
    - path: "turbo.json"
      provides: "Turborepo task pipeline (build/dev/lint/test/typecheck)"
      contains: "typecheck"
    - path: "apps/api/package.json"
      provides: "Standalone NestJS 11 project manifest with pinned @nestjs/core ^11.2.5"
      contains: "@nestjs/core"
    - path: "apps/web/src/i18n/routing.ts"
      provides: "next-intl vi/en locale routing with defaultLocale vi"
      contains: "defineRouting"
    - path: "docker-compose.yml"
      provides: "postgres/redis/minio base stack, internal network, named volumes, healthchecks"
      contains: "postgres:17-alpine"
    - path: ".env.example"
      provides: "Documented placeholder keys for every runtime secret"
      contains: "DATABASE_URL="
  key_links:
    - from: "apps/web/src/proxy.ts"
      to: "apps/web/src/i18n/routing.ts"
      via: "createMiddleware(routing)"
      pattern: "createMiddleware\\(routing\\)"
    - from: "apps/web/next.config.ts"
      to: "next-intl/plugin"
      via: "createNextIntlPlugin()"
      pattern: "createNextIntlPlugin"
    - from: "turbo.json"
      to: "apps/api + apps/web package scripts"
      via: "turbo task graph"
      pattern: "\"tasks\""
---

<objective>
Stand up the empty repository as a working pnpm + Turborepo monorepo containing a standalone NestJS 11 API, a bilingual Next.js 16 storefront, a shared-types package, and a local Docker Compose data stack (PostgreSQL 17, Redis 7, MinIO).

Purpose: this is the walking skeleton's foundation. Every subsequent plan in this phase — and every subsequent phase in the project — assumes this exact layout, these exact pinned versions, and this exact secrets discipline. Getting the wrong NestJS or Prisma major here poisons all seven phases.
Output: a repo where `pnpm turbo run typecheck` passes, `docker compose ... up -d` yields three healthy containers, and `/vi` + `/en` render locale-specific copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth-bilingual-catalog-pricing/01-RESEARCH.md
@.planning/phases/01-foundation-auth-bilingual-catalog-pricing/SKELETON.md
@CLAUDE.md

<interfaces>
<!-- No existing code. This plan creates the first interfaces. -->
<!-- packages/shared-types/src/index.ts starts as an empty barrel export; Plans 02-06 populate it. -->
<!-- Locale union used across web and api: 'vi' | 'en' (frontend) maps to Prisma enum Locale { VI EN } (backend). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — human verification before any install</name>
  <read_first>
    - `.planning/phases/01-foundation-auth-bilingual-catalog-pricing/01-RESEARCH.md` § "Package Legitimacy Audit" (every package is tagged `[ASSUMED]` because slopcheck could not be installed in this environment)
  </read_first>
  <what-built>
    Nothing yet — this gate runs BEFORE the first `pnpm add`. RESEARCH.md ran live `npm view` version checks but could NOT run slopcheck, so every package below is `[ASSUMED]` by protocol and requires human confirmation before installation.
  </what-built>
  <how-to-verify>
    Open each registry page below and confirm it is the official package described (publisher org, repository link, recent release activity, download volume in the millions/hundreds-of-thousands range):
    1. https://www.npmjs.com/package/@nestjs/core — must be published by the `nestjs` org, repo `github.com/nestjs/nest`. Confirm version `11.2.5` exists on the 11.x line and that `latest` is 12.x (we are deliberately NOT taking `latest`).
    2. https://www.npmjs.com/package/prisma — must be published by `prisma`, repo `github.com/prisma/prisma`. Confirm `latest` is an `8.0.0-rc.*` release candidate and that `7.10.0` exists (we are deliberately taking `7.10.0`).
    3. https://www.npmjs.com/package/@prisma/adapter-pg — same org, version `7.10.0`.
    4. https://www.npmjs.com/package/next — publisher `vercel`, version `16.3.5`.
    5. https://www.npmjs.com/package/react and /react-dom — publisher `facebook`/Meta, version `19.3.0`.
    6. https://www.npmjs.com/package/next-intl — version `4.14.5`, repo `github.com/amannn/next-intl`.
    7. https://www.npmjs.com/package/turbo — publisher `vercel`, version `2.11.2`.
    8. https://www.npmjs.com/package/bcrypt (6.0.0), /passport-jwt (4.0.1), /passport-local (1.0.0), /class-validator (0.15.1), /class-transformer (0.5.1), /zod (4.6.5) — all long-established community packages.
    9. https://www.npmjs.com/package/@nestjs/config — confirm `4.0.4` exists and that its peerDependency on `@nestjs/common` includes `^11.0.0`. (This package's versioning jumped 4.0.4 → 12.0.0; `@nestjs/config@11` does NOT exist. Taking `latest` here would break the Nest 11 install.)
    10. https://www.npmjs.com/package/@aws-sdk/client-s3 and /@aws-sdk/lib-storage — publisher `aws`, v3 line.
    Explicitly confirm NONE of these are typosquats and that no package named `mtn-momo*` or `xlsx` appears anywhere in this phase's dependency list.
  </how-to-verify>
  <acceptance_criteria>
    - Human has explicitly typed approval, OR named specific packages to reject/substitute.
    - This checkpoint is NOT auto-approvable; `workflow.auto_advance` does not apply to package legitimacy gates.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with installs, or name the packages to reject and their replacements.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold the pnpm + Turborepo workspace with apps/api, apps/web and packages/shared-types</name>
  <files>package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json, .gitignore, .npmrc, README.md, apps/api/** (via Nest CLI), apps/web/** (via create-next-app), packages/shared-types/package.json, packages/shared-types/tsconfig.json, packages/shared-types/src/index.ts, apps/web/next.config.ts, apps/web/src/proxy.ts, apps/web/src/i18n/routing.ts, apps/web/src/i18n/request.ts, apps/web/src/i18n/navigation.ts, apps/web/src/app/[locale]/layout.tsx, apps/web/src/app/[locale]/page.tsx, apps/web/messages/vi.json, apps/web/messages/en.json</files>
  <read_first>
    - `01-RESEARCH.md` lines 728-771 — "Monorepo scaffolding commands" and the exact `turbo.json` task pipeline
    - `01-RESEARCH.md` lines 180-210 — "Recommended Project Structure (Phase 1 additions)"
    - `01-RESEARCH.md` lines 212-215 — "Pattern A: Standalone Nest project inside a pnpm/Turborepo workspace"
    - `01-RESEARCH.md` lines 641-646 — "Pitfall 3: NestJS CLI monorepo mode confused with pnpm/Turborepo monorepo"
    - `01-RESEARCH.md` lines 573-608 — next-intl v4 App Router setup (`routing.ts`, `proxy.ts`, `request.ts`, `navigation.ts`, `next.config.ts`)
    - `01-RESEARCH.md` lines 62-95 — "Standard Stack" version table (the pinned versions below come from here)
    - `.planning/phases/01-foundation-auth-bilingual-catalog-pricing/SKELETON.md` — the architectural decisions this scaffold must match
    - `./.gitignore` — existing content, must be extended not replaced
  </read_first>
  <action>
Enable pnpm via the Node 24 bundled Corepack (`corepack enable pnpm`) — do not `npm i -g pnpm`. Verify with `pnpm --version` (expect 12.x).

Create the workspace root:
- `pnpm-workspace.yaml` listing packages `apps/*` and `packages/*`.
- Root `package.json`: `"name": "kid-toy"`, `"private": true`, `"packageManager": "pnpm@12.5.1"`, `"engines": { "node": ">=24.11" }`, and scripts `dev`/`build`/`lint`/`test`/`typecheck` each delegating to `turbo run <task>`.
- Install root dev deps with exact pins: `pnpm add -D -w turbo@2.11.2 typescript@5.9.3 prettier eslint`. If `typescript@5.9.3` does not exist, use the highest published `5.9.x` — do NOT go to 6.x (Prisma 7 / NestJS 11 / Next 16 all target 5.9).
- `turbo.json` with `$schema: https://turborepo.com/schema.json` and `tasks`: `build` (dependsOn `^build`, outputs `.next/**`, `!.next/cache/**`, `dist/**`), `dev` (cache false, persistent true), `lint` {}, `test` {}, `typecheck` {}.
- `tsconfig.base.json` with `"strict": true`, `"target": "ES2023"`, `"moduleResolution": "bundler"`, `"skipLibCheck": true`, `"noUncheckedIndexedAccess": true`. Both apps extend it.
- `.npmrc` with `auto-install-peers=true` and `strict-peer-dependencies=false`.
- Extend the existing `.gitignore` (do not overwrite) with: `node_modules/`, `.turbo/`, `dist/`, `.next/`, `*.tsbuildinfo`, `.env`, `.env.local`, `.env.*.local`, `apps/api/prisma/generated/`. Confirm `.env.deploy` is already ignored; if not, add it.

Scaffold `apps/api` as a STANDALONE Nest project — run `pnpm dlx @nestjs/cli@11 new api --package-manager pnpm --skip-git` from inside the `apps/` directory. Never run `nest generate app` at the repo root: Nest's own monorepo mode produces a conflicting root `nest-cli.json` and a nested `apps/`/`libs/` structure. After scaffolding, verify `apps/api/nest-cli.json` exists and NO `nest-cli.json` exists at the repo root. Verify `apps/api/package.json` pins `"@nestjs/core": "^11.2.5"` and `"@nestjs/common": "^11.2.5"`; if the CLI installed 12.x, explicitly downgrade with `pnpm --filter api add @nestjs/core@^11.2.5 @nestjs/common@^11.2.5 @nestjs/platform-express@^11.2.5 @nestjs/testing@^11.2.5 -D` (testing as devDependency).

Install `apps/api` runtime dependencies with EXACT major pins (bare `latest` is forbidden — `@nestjs/core@latest` resolves to 12.0.3 and `prisma@latest` resolves to 8.0.0-rc.15):
`pnpm --filter api add @nestjs/config@4.0.4 @nestjs/passport@11.0.5 @nestjs/jwt@11.0.2 @nestjs/throttler@6.7.0 @nestjs/swagger@11.4.7 passport passport-jwt@4.0.1 passport-local@1.0.0 bcrypt@6.0.0 class-validator@0.15.1 class-transformer@0.5.1 zod@4.6.5 prisma@7.10.0 @prisma/client@7.10.0 @prisma/adapter-pg@7.10.0 @aws-sdk/client-s3 @aws-sdk/lib-storage @aws-sdk/s3-request-presigner`
and dev deps: `pnpm --filter api add -D @types/bcrypt @types/passport-jwt @types/passport-local`.
Note `@nestjs/config@4.0.4` is correct for Nest 11 — that package's versioning jumped from 4.0.4 straight to 12.0.0, so `@nestjs/config@11` does not exist and `latest` would install the Nest-12-era 12.x.

Scaffold `apps/web`: `pnpm dlx create-next-app@16.3.5 apps/web --typescript --app --src-dir --eslint --import-alias "@/*" --no-tailwind --use-pnpm`. Verify `apps/web/package.json` has `next@16.3.5`, `react@19.3.0`, `react-dom@19.3.0`; pin them explicitly if the CLI resolved differently. Then `pnpm --filter web add next-intl@4.14.5`.

Wire next-intl v4 exactly as RESEARCH.md lines 573-608 specifies:
- `apps/web/src/i18n/routing.ts` — `defineRouting({ locales: ['vi','en'], defaultLocale: 'vi' })`, exported as `routing`.
- `apps/web/src/proxy.ts` — NOT `middleware.ts` (next-intl v4 renamed it). Default-export `createMiddleware(routing)` and export `config = { matcher: '/((?!api|_next|_vercel|.*\\..*).*)' }`.
- `apps/web/src/i18n/request.ts` — `getRequestConfig` resolving `requestLocale` with fallback to `routing.defaultLocale`, loading `../../messages/${locale}.json`.
- `apps/web/src/i18n/navigation.ts` — `createNavigation(routing)` exporting `Link`, `redirect`, `usePathname`, `useRouter`.
- `apps/web/next.config.ts` — `output: 'standalone'`, wrapped in `createNextIntlPlugin()(nextConfig)`.
- Move the generated root page under `apps/web/src/app/[locale]/`. `layout.tsx` must export `generateStaticParams()` returning `routing.locales.map(locale => ({ locale }))`, set `<html lang={locale}>`, and wrap children in `NextIntlClientProvider`. Delete or redirect the non-localized root page so `/` redirects to `/vi`.
- `apps/web/messages/vi.json` and `messages/en.json` each with a `Home` namespace containing `title` and `tagline` keys (vi: "Kid Toy — Đồ chơi trẻ em xuất nhập khẩu" / en: "Kid Toy — Children's Toy Import & Export"), rendered by `page.tsx` via `useTranslations('Home')`.
- Add `NEXT_PUBLIC_API_URL` usage placeholder is NOT needed yet; Plan 07 introduces the API client.

Create `packages/shared-types`: `package.json` (`"name": "@kid-toy/shared-types"`, `"version": "0.0.0"`, `"private": true`, `"main": "./src/index.ts"`, `"types": "./src/index.ts"`, scripts `typecheck: tsc --noEmit`), `tsconfig.json` extending `../../tsconfig.base.json`, and `src/index.ts` containing only `export type Locale = 'vi' | 'en';` plus `export {};` — later plans add DTO shapes here. Add `"@kid-toy/shared-types": "workspace:*"` to both `apps/api` and `apps/web` dependencies.

Add a `typecheck` script to `apps/api` (`tsc --noEmit -p tsconfig.json`) and `apps/web` (`tsc --noEmit`) so `turbo run typecheck` has real work to do.

Write a root `README.md` documenting: prerequisites (Node 24.19+, Corepack, Docker Desktop), `corepack enable pnpm`, `pnpm install`, `cp .env.example .env`, the local full-stack run command, and an explicit "never commit `.env`/`.env.deploy`" note.

After installing, run `pnpm --filter api list prisma @nestjs/core` and confirm the resolved versions are `7.10.0` and `11.2.x`. If either resolved to 8.x / 12.x, correct the pin and reinstall before proceeding.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false &amp;&amp; pnpm turbo run typecheck &amp;&amp; node -e "const a=require('./apps/api/package.json'),w=require('./apps/web/package.json');const nest=a.dependencies['@nestjs/core'],pr=a.dependencies['prisma'],nx=w.dependencies['next'];if(!/^\^?11\./.test(nest))throw new Error('BAD @nestjs/core: '+nest);if(!/^\^?7\./.test(pr))throw new Error('BAD prisma: '+pr);if(!/^\^?16\./.test(nx))throw new Error('BAD next: '+nx);console.log('versions ok',nest,pr,nx)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` exists at repo root and lists `apps/*` and `packages/*`.
    - `apps/api/nest-cli.json` exists; `./nest-cli.json` (repo root) does NOT exist.
    - `apps/api/package.json` `dependencies["@nestjs/core"]` matches `^11.` and `dependencies["prisma"]` matches `^7.`; neither is `latest`, `*`, `^12.`, or `^8.`.
    - `apps/api/package.json` `dependencies["@nestjs/config"]` is exactly `4.0.4`.
    - `apps/web/package.json` `dependencies["next"]` matches `^16.` and `dependencies["next-intl"]` matches `^4.`.
    - `apps/web/src/proxy.ts` exists and `apps/web/src/middleware.ts` does NOT exist.
    - `apps/web/src/i18n/routing.ts` contains `defineRouting` with `locales: ['vi', 'en']` and `defaultLocale: 'vi'`.
    - `apps/web/src/app/[locale]/layout.tsx` exports `generateStaticParams`.
    - `apps/web/messages/vi.json` and `apps/web/messages/en.json` both exist and contain a `Home.title` key with different values.
    - `packages/shared-types/src/index.ts` exists and both apps list `"@kid-toy/shared-types": "workspace:*"`.
    - `pnpm turbo run typecheck` exits 0.
    - `git grep -n -E "(DEPLOY_PASSWORD|POSTGRES_PASSWORD|JWT_[A-Z]*SECRET)\s*=\s*[^\s\"'$]" -- ':!*.example'` returns no matches (no literal secret values committed).
  </acceptance_criteria>
  <done>Root `pnpm turbo run typecheck` passes; `pnpm --filter web dev` serves `/vi` and `/en` with locale-specific copy; version pins verified as NestJS 11.x / Prisma 7.x / Next 16.x.</done>
</task>

<task type="auto">
  <name>Task 3: Local Docker Compose data stack (PostgreSQL 17, Redis 7, MinIO) plus .env.example</name>
  <files>docker-compose.yml, docker-compose.dev.yml, .env.example, README.md</files>
  <read_first>
    - `01-RESEARCH.md` lines 659-726 — the Docker Compose skeleton (base + dev override) with healthchecks, named volumes and the `${VAR:?err}` fail-fast pattern
    - `01-RESEARCH.md` lines 810-825 — "Environment Availability": Docker was NOT found installed on the machine that ran research
    - `.planning/research/PITFALLS.md` lines 129-151 — Pitfall 6 (restart policy, named volumes, never publish DB ports on a public interface; Docker's iptables rules bypass ufw)
    - `./CLAUDE.md` — the "never commit secrets" constraint
    - `./.env.deploy` — for the KEY NAMES only (`DEPLOY_HOST`, `DEPLOY_USER`, `DEPLOY_PASSWORD`, `DEPLOY_PATH`, `DEPLOY_PLATFORM`). Never copy any value from this file into a tracked file.
  </read_first>
  <action>
FIRST, verify the Docker prerequisite: run `docker --version` and `docker compose version`. RESEARCH.md recorded Docker as NOT installed on this machine. If either command fails, STOP this task immediately and report: "BLOCKED: Docker Engine + Compose v2 is not installed. Install Docker Desktop for Windows (or Docker Engine + the compose plugin), start it, and re-run this plan." Do not attempt to install Docker yourself and do not fake the verification.

Create `docker-compose.yml` (base — internal network only, NO host port publishing) with `name: kid-toy` and three services:
- `postgres`: image `postgres:17-alpine`, `restart: unless-stopped`, environment `POSTGRES_DB: ${POSTGRES_DB:-kidtoy}`, `POSTGRES_USER: ${POSTGRES_USER:-kidtoy}`, `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}` (fail fast — never default a real password), named volume `pg_data:/var/lib/postgresql/data`, healthcheck `["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-kidtoy}"]` interval 5s timeout 5s retries 10, network `internal`.
- `redis`: image `redis:7-alpine`, `restart: unless-stopped`, named volume `redis_data:/data`, healthcheck `["CMD", "redis-cli", "ping"]`, network `internal`.
- `minio`: image `minio/minio:latest`, `restart: unless-stopped`, command `server /data --console-address ":9001"`, environment `MINIO_ROOT_USER: ${MINIO_ROOT_USER:?required}` and `MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?required}`, named volume `minio_data:/data`, healthcheck `["CMD", "mc", "ready", "local"]`, network `internal`.
Declare `networks: internal: {}` and `volumes: pg_data: {} redis_data: {} minio_data: {}`. No literal credential value appears anywhere in this file — only `${VAR}` interpolation.

Create `docker-compose.dev.yml` as a LOCAL-ONLY override publishing ports bound explicitly to `127.0.0.1`, never `0.0.0.0`: postgres `"127.0.0.1:5432:5432"`, redis `"127.0.0.1:6379:6379"`, minio `"127.0.0.1:9000:9000"` and `"127.0.0.1:9001:9001"`. Add a header comment citing PITFALLS.md Pitfall 6 (Docker manipulates iptables directly and bypasses ufw, so a `0.0.0.0` bind on a VPS would expose Postgres publicly even behind a firewall).

Create `.env.example` at the repo root — placeholder values only, this file IS committed. Keys, grouped with comments:
`POSTGRES_DB=kidtoy`, `POSTGRES_USER=kidtoy`, `POSTGRES_PASSWORD=change-me-locally`,
`DATABASE_URL=postgresql://kidtoy:change-me-locally@localhost:5432/kidtoy?schema=public`,
`SHADOW_DATABASE_URL=postgresql://kidtoy:change-me-locally@localhost:5432/kidtoy_shadow?schema=public`,
`REDIS_URL=redis://localhost:6379`,
`MINIO_ROOT_USER=minioadmin`, `MINIO_ROOT_PASSWORD=change-me-locally`, `MINIO_ENDPOINT=http://localhost:9000`, `MINIO_REGION=us-east-1`, `MINIO_BUCKET_MEDIA=kidtoy-media`, `MINIO_BUCKET_DOCUMENTS=kidtoy-documents`,
`JWT_ACCESS_SECRET=generate-with-openssl-rand-hex-32`, `JWT_REFRESH_SECRET=generate-with-a-different-openssl-rand-hex-32`, `JWT_ACCESS_TTL=15m`, `JWT_REFRESH_TTL=30d`,
`PASSWORD_RESET_TTL_MINUTES=60`, `BCRYPT_COST=12`,
`API_PORT=4000`, `API_CORS_ORIGIN=http://localhost:3000`, `APP_BASE_URL=http://localhost:3000`,
`NEXT_PUBLIC_API_URL=http://localhost:4000`.
Add a top-of-file comment: "Copy to `.env` and fill real values. `.env` is gitignored. Deployment credentials live ONLY in `.env.deploy` (also gitignored) — never in this file, never in docker-compose*.yml, never in a Dockerfile."

Create a local `.env` from `.env.example` (it is gitignored) with working local values so the stack can actually start, then run `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d` and poll until all three containers report `healthy`.

Append a "Local development" section to `README.md`: the exact `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d` command, the `pnpm turbo run dev` command, and a note that this pair IS the documented local full-stack run.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.dev.yml config --quiet &amp;&amp; docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d &amp;&amp; sleep 25 &amp;&amp; docker compose ps --format json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const rows=s.trim().split('\n').filter(Boolean).map(JSON.parse);const bad=rows.filter(r=>!/healthy/.test(r.Health||r.State||''));if(rows.length<3||bad.length)throw new Error('unhealthy: '+JSON.stringify(rows.map(r=>[r.Service,r.Health||r.State])));console.log('3/3 healthy')})"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f docker-compose.yml -f docker-compose.dev.yml config --quiet` exits 0.
    - `docker-compose.yml` contains `image: postgres:17-alpine`, `image: redis:7-alpine`, `image: minio/minio`, and `restart: unless-stopped` appears exactly 3 times.
    - `docker-compose.yml` declares named volumes `pg_data`, `redis_data`, `minio_data` and contains NO `ports:` key.
    - Every `ports:` entry in `docker-compose.dev.yml` is prefixed `127.0.0.1:`; `grep -c '0.0.0.0' docker-compose.dev.yml` returns 0.
    - `docker compose ps` shows `postgres`, `redis`, `minio` all with health state `healthy`.
    - `.env.example` exists and contains the keys `DATABASE_URL=`, `JWT_ACCESS_SECRET=`, `MINIO_BUCKET_DOCUMENTS=`, `NEXT_PUBLIC_API_URL=`.
    - `git check-ignore -q .env` exits 0 (`.env` is ignored) and `git ls-files .env` returns nothing.
    - `grep -v '^#' docker-compose.yml | grep -cE "PASSWORD:\s*[A-Za-z0-9]" ` returns 0 (no literal password value, only `${VAR}` interpolation).
  </acceptance_criteria>
  <done>Three healthy containers running locally; `.env.example` committed with placeholders only; no secret value in any tracked file; README documents the local full-stack run command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules` | Third-party package code executes at install and at runtime with full process privilege |
| Docker host → container network | Published ports cross from the container network to a host interface; on a VPS a `0.0.0.0` bind reaches the public internet |
| Developer workstation → git remote | Any file written here can be permanently published if it contains a secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `pnpm add` of `@nestjs/*`, `prisma`, `next`, `next-intl`, `bcrypt`, AWS SDK | mitigate | slopcheck unavailable → all packages `[ASSUMED]`; Task 1 is a blocking human-verify gate against each npm registry page BEFORE any install. Not auto-approvable. |
| T-01-02 | Tampering | Dependency major-version drift | mitigate | Every install is version-pinned from RESEARCH.md's live `npm view` checks. Task 2's automated verify asserts `@nestjs/core ^11.`, `prisma ^7.`, `next ^16.` and fails the task otherwise — bare `latest` silently installs NestJS 12 / Prisma 8 RC. |
| T-01-03 | Information Disclosure | `docker-compose.yml`, `.env.example`, `README.md` committed to git | mitigate | Compose files use only `${VAR:?err}` interpolation; `.env.example` holds placeholders only; `.env` and `.env.deploy` are gitignored; Task 3 acceptance asserts `git ls-files .env` is empty and no literal password appears in compose. |
| T-01-04 | Information Disclosure | PostgreSQL / Redis / MinIO port exposure | mitigate | Base `docker-compose.yml` publishes NO host ports. The dev override binds every port to `127.0.0.1` explicitly, never `0.0.0.0` — Docker writes iptables rules directly and bypasses ufw, so a `0.0.0.0` bind on the VPS would be publicly reachable despite the firewall (PITFALLS.md Pitfall 6). |
| T-01-05 | Denial of Service | Container crash on the single VPS | mitigate | `restart: unless-stopped` on all three services plus healthchecks, so `depends_on` ordering in later plans reflects real readiness rather than "container started". |
| T-01-06 | Repudiation | Loss of database volume | accept | Named volumes (not ad-hoc bind mounts) are in place; an automated off-VPS `pg_dump` + rehearsed restore is carried forward as a known concern to close before real customer data exists. No production data exists in Phase 1. |
</threat_model>

<verification>
1. `pnpm turbo run typecheck` exits 0 across `api`, `web` and `shared-types`.
2. `pnpm --filter api list prisma @nestjs/core` reports `7.10.x` and `11.2.x`.
3. `docker compose -f docker-compose.yml -f docker-compose.dev.yml ps` shows three `healthy` services.
4. `pnpm --filter web dev` then `curl -s localhost:3000/vi` and `curl -s localhost:3000/en` return different, locale-correct title strings.
5. `git status --porcelain` shows no `.env` and no `.env.deploy` staged.
</verification>

<success_criteria>
- Repo is a working pnpm + Turborepo monorepo with `apps/api` (standalone NestJS 11.2.x), `apps/web` (Next.js 16.3.5 + next-intl 4.14.5, `vi`/`en` routing) and `packages/shared-types`.
- Every dependency is version-pinned to the majors RESEARCH.md verified; no bare `latest` install occurred.
- Local Docker stack (postgres:17-alpine, redis:7-alpine, minio) starts healthy with named volumes, `restart: unless-stopped`, and loopback-only port binds.
- `.env.example` documents all required keys with placeholders; no secret value exists in any git-tracked file.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-bilingual-catalog-pricing/01-01-SUMMARY.md` when done.
</output>
