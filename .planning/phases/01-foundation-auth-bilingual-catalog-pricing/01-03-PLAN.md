---
phase: 01-foundation-auth-bilingual-catalog-pricing
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - apps/api/test/auth.e2e-spec.ts
  - apps/api/test/utils/test-app.ts
  - apps/api/test/jest-e2e.json
  - apps/api/src/modules/auth/auth.module.ts
  - apps/api/src/modules/auth/auth.controller.ts
  - apps/api/src/modules/auth/auth.service.ts
  - apps/api/src/modules/auth/token.service.ts
  - apps/api/src/modules/auth/token.service.spec.ts
  - apps/api/src/modules/auth/dto/register-customer.dto.ts
  - apps/api/src/modules/auth/dto/login.dto.ts
  - apps/api/src/modules/auth/dto/refresh.dto.ts
  - apps/api/src/modules/auth/strategies/local.strategy.ts
  - apps/api/src/modules/auth/strategies/jwt.strategy.ts
  - apps/api/src/common/guards/jwt-auth.guard.ts
  - apps/api/src/common/guards/optional-jwt-auth.guard.ts
  - apps/api/src/common/guards/roles.guard.ts
  - apps/api/src/common/guards/roles.guard.spec.ts
  - apps/api/src/common/decorators/roles.decorator.ts
  - apps/api/src/common/decorators/current-user.decorator.ts
  - apps/api/src/common/types/jwt-payload.ts
  - apps/api/src/modules/admin/admin-probe.controller.ts
  - apps/api/src/app.module.ts
  - packages/shared-types/src/index.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-04]

must_haves:
  truths:
    - "A seeded staff account can POST /api/auth/login with email+password and receive an access token plus a refresh token"
    - "A session survives a browser refresh: the refresh token can be exchanged for a new access token, and the old refresh token stops working"
    - "A new retail customer can register with email+password and immediately log in"
    - "A staff account whose role is WAREHOUSE receives 403 from a SUPER_ADMIN-only endpoint; a SUPER_ADMIN receives 200"
    - "An anonymous request to an OptionalJwtAuthGuard-protected route succeeds with no user rather than 401"
    - "Login with a wrong password returns 401 and never reveals whether the email exists"
  artifacts:
    - path: "apps/api/src/modules/auth/auth.service.ts"
      provides: "bcrypt credential validation, customer registration, login orchestration"
      exports: ["AuthService"]
    - path: "apps/api/src/modules/auth/token.service.ts"
      provides: "Access JWT issuance and rotating, hashed, revocable refresh tokens"
      exports: ["TokenService"]
    - path: "apps/api/src/common/guards/roles.guard.ts"
      provides: "Deny-by-default staff RBAC"
      exports: ["RolesGuard"]
    - path: "apps/api/src/common/guards/optional-jwt-auth.guard.ts"
      provides: "Anonymous-tolerant JWT guard for public catalog routes"
      exports: ["OptionalJwtAuthGuard"]
    - path: "apps/api/test/auth.e2e-spec.ts"
      provides: "End-to-end proof of AUTH-01, AUTH-02, AUTH-04"
      min_lines: 80
  key_links:
    - from: "apps/api/src/modules/auth/token.service.ts"
      to: "refresh_tokens.tokenHash"
      via: "sha256(rawToken) stored, raw token returned to client only"
      pattern: "createHash\\('sha256'\\)"
    - from: "apps/api/src/common/guards/roles.guard.ts"
      to: "apps/api/src/common/decorators/roles.decorator.ts"
      via: "Reflector.getAllAndOverride(ROLES_KEY)"
      pattern: "ROLES_KEY"
    - from: "apps/api/src/modules/auth/strategies/jwt.strategy.ts"
      to: "JWT_ACCESS_SECRET"
      via: "ConfigService.getOrThrow"
      pattern: "JWT_ACCESS_SECRET"
---

<objective>
Deliver the first real user-visible capability of the walking skeleton: every principal type — staff, retail customer, and (from Plan 04) business applicant — authenticates through ONE `AuthModule` against ONE `Account` table, with staff role-based access control enforced at the API layer.

Purpose: AUTH-01 requires a session that survives a refresh, which means rotating refresh tokens, not just an access JWT. AUTH-04 requires RBAC that is enforced server-side — PITFALLS.md is explicit that a hidden UI button is not access control. Every later phase's admin, dealer and customer surface hangs off the guards created here.
Output: `POST /api/auth/register`, `/login`, `/refresh`, `/logout`, `GET /api/auth/me`, and a role-gated probe endpoint proving 403-vs-200.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth-bilingual-catalog-pricing/01-RESEARCH.md
@.planning/phases/01-foundation-auth-bilingual-catalog-pricing/SKELETON.md
@.planning/phases/01-foundation-auth-bilingual-catalog-pricing/01-02-SUMMARY.md
@apps/api/prisma/schema.prisma
@apps/api/src/prisma/prisma.service.ts
@apps/api/src/config/env.schema.ts

<interfaces>
<!-- Contracts this plan CREATES. Plans 04-08 are written against these exact names. -->

apps/api/src/common/types/jwt-payload.ts:
  export interface JwtPayload {
    sub: string;                                                  // Account.id
    type: 'STAFF' | 'RETAIL_CUSTOMER' | 'BUSINESS_ACCOUNT';
    role?: 'SUPER_ADMIN' | 'SALES' | 'WAREHOUSE' | 'CONTENT';     // present only when type === 'STAFF'
    approvalStatus?: 'PENDING' | 'APPROVED' | 'REJECTED';         // present only when type === 'BUSINESS_ACCOUNT'
    priceTierId?: string | null;                                  // present only when type === 'BUSINESS_ACCOUNT' and APPROVED
  }

apps/api/src/common/decorators/roles.decorator.ts:
  export const ROLES_KEY = 'roles';
  export const Roles = (...roles: StaffRole[]) => SetMetadata(ROLES_KEY, roles);

apps/api/src/common/decorators/current-user.decorator.ts:
  export const CurrentUser = createParamDecorator(...): JwtPayload | undefined

apps/api/src/common/guards/jwt-auth.guard.ts        -> class JwtAuthGuard extends AuthGuard('jwt')          // 401 on missing/invalid token
apps/api/src/common/guards/optional-jwt-auth.guard.ts -> class OptionalJwtAuthGuard extends AuthGuard('jwt') // handleRequest returns undefined instead of throwing
apps/api/src/common/guards/roles.guard.ts           -> class RolesGuard implements CanActivate

apps/api/src/modules/auth/token.service.ts:
  issueAccessToken(payload: JwtPayload): Promise<string>
  issueRefreshToken(accountId: string): Promise<string>                 // returns RAW token; stores sha256 hash
  rotateRefreshToken(rawToken: string): Promise<{ accessToken: string; refreshToken: string }>
  revokeAllForAccount(accountId: string): Promise<number>

HTTP contract (global prefix `api`):
  POST /api/auth/register  { email, password, fullName?, phone? }  -> 201 { accessToken, refreshToken, account }
  POST /api/auth/login     { email, password }                     -> 200 { accessToken, refreshToken, account }
  POST /api/auth/refresh   { refreshToken }                        -> 200 { accessToken, refreshToken }
  POST /api/auth/logout    Bearer                                  -> 204
  GET  /api/auth/me        Bearer                                  -> 200 { id, email, type, role?, approvalStatus?, priceTierId? }
  GET  /api/admin/_probe/super-admin  Bearer                       -> 200 (SUPER_ADMIN only) | 403
  GET  /api/admin/_probe/content      Bearer                       -> 200 (SUPER_ADMIN or CONTENT) | 403

Shared types added to packages/shared-types/src/index.ts:
  export interface AuthenticatedAccount { id: string; email: string; type: AccountType; role?: StaffRole; approvalStatus?: 'PENDING'|'APPROVED'|'REJECTED'; priceTierId?: string | null }
  export interface AuthTokens { accessToken: string; refreshToken: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing end-to-end auth spec (RED) — the happy path for AUTH-01, AUTH-02, AUTH-04</name>
  <files>apps/api/test/auth.e2e-spec.ts, apps/api/test/utils/test-app.ts, apps/api/test/jest-e2e.json, apps/api/package.json</files>
  <read_first>
    - `01-RESEARCH.md` lines 827-860 — "Validation Architecture": Jest is the framework, Wave 0 must create the shared fixture/test-DB reset strategy, and the per-requirement test map
    - `apps/api/prisma/seed.ts` — the exact seeded emails and roles this spec asserts against
    - `apps/api/test/` — whatever `nest new` generated (typically `app.e2e-spec.ts` + `jest-e2e.json`)
  </read_first>
  <action>
Create `apps/api/test/utils/test-app.ts` exporting `createTestApp()`, which builds the full `AppModule` via `Test.createTestingModule`, applies the SAME global configuration as `main.ts` (global `api` prefix excluding `health`, `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true, transform: true })`, the `BigInt.prototype.toJSON` shim), calls `init()`, and returns `{ app, prisma }`. Also export `resetAndSeed(prisma)` which truncates all tables in FK-safe order (or `TRUNCATE ... RESTART IDENTITY CASCADE`) and then runs the seed logic, and `uniqueEmail(prefix)` returning `${prefix}+${randomUUID()}@test.local`.

Tests run against a dedicated database, never the dev database: `test-app.ts` must assert that `process.env.DATABASE_URL` ends with `kidtoy_test` and throw a clear error otherwise. Add a `test:e2e` script in `apps/api/package.json` that loads `.env.test` (create `.env.test.example` with `DATABASE_URL=postgresql://kidtoy:change-me-locally@localhost:5432/kidtoy_test?schema=public` and the same JWT/MinIO keys) and runs `jest --config test/jest-e2e.json --runInBand`. Create the `kidtoy_test` database and apply migrations to it.

Write `apps/api/test/auth.e2e-spec.ts` using `supertest`. It MUST fail right now (no `AuthModule` exists yet) — that is the point of this task. Cases:
  1. AUTH-02 register: `POST /api/auth/register` with a unique email and password `Str0ng-Passw0rd!` returns 201, a body containing `accessToken` and `refreshToken` (both non-empty strings) and `account.type === 'RETAIL_CUSTOMER'`; the response body contains NO `passwordHash` key anywhere.
  2. AUTH-02 register rejects a duplicate email with 409.
  3. AUTH-02 register rejects a password shorter than 10 characters with 400.
  4. AUTH-02 login: the just-registered customer can `POST /api/auth/login` and receives 200 with tokens.
  5. AUTH-01 staff login: `admin@kidtoy.local` with `SEED_DEFAULT_PASSWORD` returns 200; the decoded access token payload has `type === 'STAFF'` and `role === 'SUPER_ADMIN'`.
  6. AUTH-01 session persists across refresh: `POST /api/auth/refresh` with the login's refresh token returns 200 with a NEW access token AND a NEW refresh token; the returned access token authenticates `GET /api/auth/me` with 200.
  7. AUTH-01 refresh rotation: replaying the ORIGINAL refresh token after step 6 returns 401.
  8. AUTH-01 logout: `POST /api/auth/logout` returns 204, after which the most recent refresh token returns 401 from `/api/auth/refresh`.
  9. Wrong password returns 401, and the response body is byte-identical to the response for a non-existent email (no user enumeration).
  10. AUTH-04 RBAC positive: `admin@kidtoy.local` gets 200 from `GET /api/admin/_probe/super-admin`.
  11. AUTH-04 RBAC negative: `warehouse@kidtoy.local` gets 403 from `GET /api/admin/_probe/super-admin`, and `content@kidtoy.local` gets 200 from `GET /api/admin/_probe/content` but 403 from `/super-admin`.
  12. AUTH-04 deny-by-default: an unauthenticated request to `GET /api/admin/_probe/super-admin` returns 401 (not 403, not 200).
  13. Non-staff principal: the seeded retail customer gets 403 from both probe endpoints even though they hold a valid token.

Install `supertest` and `@types/supertest` in `apps/api` if `nest new` did not include them.
  </action>
  <verify>
    <automated>pnpm --filter api run test:e2e -- auth.e2e-spec.ts; test $? -ne 0 &amp;&amp; echo "RED as expected" || (echo "spec passed before implementation — spec is not asserting anything" &amp;&amp; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/test/auth.e2e-spec.ts` exists with at least 13 `it(...)` cases covering the numbered list above.
    - `apps/api/test/utils/test-app.ts` exports `createTestApp`, `resetAndSeed` and `uniqueEmail`, and throws if `DATABASE_URL` does not target `kidtoy_test`.
    - `apps/api/package.json` has a `test:e2e` script.
    - `.env.test.example` exists and is committed; `.env.test` is gitignored.
    - Running `pnpm --filter api run test:e2e -- auth.e2e-spec.ts` exits NON-ZERO with failures referencing missing `/api/auth/*` routes (RED state confirmed, not a config error).
  </acceptance_criteria>
  <done>A complete, currently-failing end-to-end specification of AUTH-01, AUTH-02 and AUTH-04 exists, running against an isolated `kidtoy_test` database.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AuthModule — registration, login, and rotating hashed refresh tokens (GREEN for AUTH-01, AUTH-02)</name>
  <files>apps/api/src/modules/auth/auth.module.ts, apps/api/src/modules/auth/auth.controller.ts, apps/api/src/modules/auth/auth.service.ts, apps/api/src/modules/auth/token.service.ts, apps/api/src/modules/auth/token.service.spec.ts, apps/api/src/modules/auth/dto/register-customer.dto.ts, apps/api/src/modules/auth/dto/login.dto.ts, apps/api/src/modules/auth/dto/refresh.dto.ts, apps/api/src/modules/auth/strategies/local.strategy.ts, apps/api/src/modules/auth/strategies/jwt.strategy.ts, apps/api/src/common/types/jwt-payload.ts, apps/api/src/common/guards/jwt-auth.guard.ts, apps/api/src/common/guards/optional-jwt-auth.guard.ts, apps/api/src/common/decorators/current-user.decorator.ts, apps/api/src/app.module.ts, packages/shared-types/src/index.ts</files>
  <read_first>
    - `01-RESEARCH.md` lines 217-273 — "Pattern B": the `JwtStrategy` implementation and `JwtPayload` shape
    - `01-RESEARCH.md` line 315 — the `OptionalJwtAuthGuard` requirement (default `AuthGuard('jwt')` 401s anonymous catalog visitors)
    - `01-RESEARCH.md` lines 617-627 — "Don't Hand-Roll": bcrypt cost ≥ 12, `@nestjs/jwt` + `passport-jwt`, `crypto.randomBytes(32)` + SHA-256-at-rest for refresh tokens, never `Math.random()`
    - `01-RESEARCH.md` lines 862-883 — ASVS V2/V3/V6 controls and the credential-stuffing / JWT-algorithm-confusion / reset-replay threat patterns
    - `apps/api/prisma/schema.prisma` — `Account`, `StaffProfile`, `CustomerProfile`, `BusinessAccount`, `RefreshToken` field names
    - `apps/api/src/config/env.schema.ts` — `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `JWT_ACCESS_TTL`, `JWT_REFRESH_TTL`, `BCRYPT_COST`
    - `apps/api/test/auth.e2e-spec.ts` — the exact contract this task must satisfy
  </read_first>
  <behavior>
    - `TokenService.issueRefreshToken` returns a raw 64-hex-char token and persists ONLY its SHA-256 hash; a direct `refresh_tokens` row lookup by the raw value finds nothing.
    - `TokenService.rotateRefreshToken` with a valid unrevoked unexpired token returns new tokens and sets `revokedAt` on the presented token.
    - `TokenService.rotateRefreshToken` throws `UnauthorizedException` for: an unknown token, an already-revoked token, and an expired token.
    - `revokeAllForAccount` revokes every outstanding token for that account and returns the count.
    - `AuthService.validateCredentials` returns null (never throws a distinguishable error) for both a wrong password and an unknown email; the controller maps both to an identical 401 body.
    - Registering with an existing email yields 409; registering always creates `Account.type = RETAIL_CUSTOMER` plus a `CustomerProfile`, never a `StaffProfile`.
  </behavior>
  <action>
Create `apps/api/src/common/types/jwt-payload.ts` with the `JwtPayload` interface exactly as declared in this plan's `<interfaces>` block.

Create `TokenService`:
  - `issueAccessToken(payload)` signs with `JwtService` using `secret: JWT_ACCESS_SECRET`, `expiresIn: JWT_ACCESS_TTL` and an EXPLICIT `algorithm: 'HS256'` (pinning the algorithm defends against `alg: none` / algorithm-confusion).
  - `issueRefreshToken(accountId)` generates `randomBytes(32).toString('hex')`, stores `createHash('sha256').update(raw).digest('hex')` in `refresh_tokens.tokenHash` with `expiresAt = now + JWT_REFRESH_TTL`, and returns the RAW token. The raw value is never written to the database or to a log line.
  - `rotateRefreshToken(raw)` hashes the input, looks the row up by `tokenHash`, rejects with `UnauthorizedException` when not found / `revokedAt != null` / `expiresAt <= now`, then inside a single `prisma.$transaction` sets `revokedAt = now` on the old row and issues a new refresh token, and re-issues an access token from a freshly-read `Account` (so a role or approval change takes effect on refresh).
  - `revokeAllForAccount(accountId)` sets `revokedAt = now` on all non-revoked rows.
Write `token.service.spec.ts` covering every behavior bullet above, including an explicit assertion that `prisma.refreshToken.findUnique({ where: { tokenHash: rawToken } })` returns null (i.e. the raw token is not what is stored).

Create `AuthService`:
  - `validateCredentials(email, password)` loads the `Account` with its `staffProfile`/`customerProfile`/`businessAccount` relations. If the account is missing or `isActive === false`, still run a `bcrypt.compare` against a constant dummy hash before returning null, so the timing of a miss matches the timing of a hit.
  - `buildPayload(account)` produces the `JwtPayload`: always `sub` and `type`; `role` only for STAFF; `approvalStatus` and `priceTierId` only for BUSINESS_ACCOUNT (and `priceTierId` only when `approvalStatus === 'APPROVED'`).
  - `registerCustomer(dto)` hashes the password with `bcrypt` at `BCRYPT_COST` (minimum 12), creates `Account` + `CustomerProfile` in one `$transaction`, and maps a Prisma `P2002` unique violation on `email` to `ConflictException` (409).
  - `login(account)` returns `{ accessToken, refreshToken, account: <safe projection> }`. The safe projection is `{ id, email, type, role?, approvalStatus?, priceTierId? }` — `passwordHash` is never included in any response, log, or Swagger example.

Create DTOs with `class-validator`: `RegisterCustomerDto` (`@IsEmail` email, `@IsString @MinLength(10) @MaxLength(128)` password, optional `@IsString @MaxLength(120)` fullName, optional `@Matches(/^0\d{9,10}$/)` phone), `LoginDto` (`@IsEmail`, `@IsString @IsNotEmpty` password), `RefreshDto` (`@IsString @Length(64,64)` refreshToken).

Create `LocalStrategy` (`usernameField: 'email'`) delegating to `AuthService.validateCredentials` and throwing `UnauthorizedException` with a generic message on null. Create `JwtStrategy` per RESEARCH.md lines 222-247: `ExtractJwt.fromAuthHeaderAsBearerToken()`, `ignoreExpiration: false`, `secretOrKey: config.getOrThrow('JWT_ACCESS_SECRET')`, plus `algorithms: ['HS256']`. Its `validate(payload)` returns the payload as `request.user`.

Create `JwtAuthGuard extends AuthGuard('jwt')` and `OptionalJwtAuthGuard extends AuthGuard('jwt')` — the optional variant overrides `handleRequest(err, user)` to return `user ?? undefined` and never throw, so anonymous catalog browsing works. Create the `CurrentUser` param decorator returning `request.user as JwtPayload | undefined`.

Create `AuthController` (`@Controller('auth')`) implementing the five routes from the `<interfaces>` HTTP contract. Apply `@Throttle({ default: { limit: 5, ttl: 60_000 } })` to `login` and `register` specifically (tighter than the global 120/min) per the credential-stuffing threat. `logout` is `@UseGuards(JwtAuthGuard)`, returns 204, and calls `revokeAllForAccount(user.sub)`. `me` is `@UseGuards(JwtAuthGuard)` and re-reads the account from the database rather than trusting the token body. Annotate every route with `@ApiOperation`/`@ApiResponse` for Swagger.

Create `AuthModule` importing `PassportModule`, `JwtModule.register({})` (secrets are passed per-sign call, not registered globally, because access and refresh use different secrets), and `PrismaModule`; it provides `AuthService`, `TokenService`, `LocalStrategy`, `JwtStrategy` and EXPORTS `AuthService` and `TokenService`. Register `AuthModule` in `AppModule`.

Add `AuthenticatedAccount` and `AuthTokens` to `packages/shared-types/src/index.ts` exactly as declared in `<interfaces>`.
  </action>
  <verify>
    <automated>pnpm --filter api test -- token.service.spec.ts --watch=false &amp;&amp; pnpm --filter api run test:e2e -- auth.e2e-spec.ts -t "AUTH-01|AUTH-02|login|register|refresh|logout"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter api test -- token.service.spec.ts` passes with a case asserting the raw refresh token is NOT stored.
    - e2e cases 1-9 from Task 1 pass.
    - `apps/api/src/modules/auth/token.service.ts` contains `randomBytes(32)` and `createHash('sha256')`; it does NOT contain `Math.random`.
    - `apps/api/src/modules/auth/auth.service.ts` references `BCRYPT_COST` and the resolved cost is `>= 12`.
    - `apps/api/src/modules/auth/strategies/jwt.strategy.ts` contains `algorithms: ['HS256']` and `getOrThrow` for `JWT_ACCESS_SECRET`; no secret literal appears in any source file (`grep -rn --include=*.ts -E "secret\s*[:=]\s*['\"][A-Za-z0-9+/=]{16,}" apps/api/src` returns nothing).
    - `apps/api/src/common/guards/optional-jwt-auth.guard.ts` overrides `handleRequest` and returns `undefined` rather than throwing.
    - `curl -s -X POST localhost:4000/api/auth/login -H 'content-type: application/json' -d '{"email":"nope@x.local","password":"whatever12"}'` and the same call with a real email + wrong password produce byte-identical response bodies and both return 401.
    - `grep -rn "passwordHash" apps/api/src/modules/auth/auth.controller.ts` returns no matches.
  </acceptance_criteria>
  <done>Staff and retail customers authenticate through one module; sessions survive a refresh via rotating, hashed, revocable refresh tokens; login is rate-limited and does not leak whether an email exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deny-by-default staff RBAC (GREEN for AUTH-04)</name>
  <files>apps/api/src/common/decorators/roles.decorator.ts, apps/api/src/common/guards/roles.guard.ts, apps/api/src/common/guards/roles.guard.spec.ts, apps/api/src/modules/admin/admin-probe.controller.ts, apps/api/src/app.module.ts</files>
  <read_first>
    - `01-RESEARCH.md` lines 249-273 — the `@Roles()` decorator and `RolesGuard` reference implementation, and the documented usage `@UseGuards(JwtAuthGuard, RolesGuard) @Roles('SUPER_ADMIN','CONTENT')`
    - `01-RESEARCH.md` lines 866-871 — ASVS V4: deny-by-default, every mutating admin endpoint tested for 403 with an under-privileged role
    - `.planning/research/PITFALLS.md` lines 256-264 — "Default-permissive RBAC" security mistake, and line 285 — "RBAC looks done when roles exist in the admin UI; verify the block happens at the API layer"
    - `apps/api/src/common/types/jwt-payload.ts` and `apps/api/src/common/guards/jwt-auth.guard.ts` (from Task 2)
    - `apps/api/test/auth.e2e-spec.ts` — RBAC cases 10-13
  </read_first>
  <behavior>
    - `RolesGuard` returns true when `@Roles()` metadata is absent (the route's own `JwtAuthGuard` is what enforces authentication there) — but `admin-probe.controller.ts` always declares `@Roles()`, so the probe routes are never open.
    - `RolesGuard` returns false when `user` is undefined, when `user.type !== 'STAFF'`, or when `user.role` is not in the required list.
    - `RolesGuard` returns true only when `user.type === 'STAFF'` AND `user.role` is in the required list.
    - A request with a valid token but an insufficient role yields 403; a request with no token yields 401 (guard order `JwtAuthGuard, RolesGuard` produces this).
  </behavior>
  <action>
Create `apps/api/src/common/decorators/roles.decorator.ts` exporting `ROLES_KEY = 'roles'` and `Roles(...roles: StaffRole[])` via `SetMetadata`, typed against the `StaffRole` union from `@kid-toy/shared-types` (not `string[]`, so a typo like `'SUPERADMIN'` fails to compile).

Create `apps/api/src/common/guards/roles.guard.ts` per RESEARCH.md lines 255-271: inject `Reflector`, read `ROLES_KEY` with `getAllAndOverride([ctx.getHandler(), ctx.getClass()])`, return `true` when no metadata is set, otherwise return `user?.type === 'STAFF' && required.includes(user.role)`. Throw nothing — returning false lets Nest produce the 403.

Write `apps/api/src/common/guards/roles.guard.spec.ts` as a pure unit test with a mocked `Reflector` and `ExecutionContext`, covering every behavior bullet: no metadata → true; undefined user → false; RETAIL_CUSTOMER with a role-shaped token → false; STAFF/WAREHOUSE against `['SUPER_ADMIN']` → false; STAFF/SUPER_ADMIN against `['SUPER_ADMIN']` → true; STAFF/CONTENT against `['SUPER_ADMIN','CONTENT']` → true.

Create `apps/api/src/modules/admin/admin-probe.controller.ts` — `@Controller('admin/_probe')`, `@ApiBearerAuth()`, class-level `@UseGuards(JwtAuthGuard, RolesGuard)` (order matters: authentication first, then authorization). Two routes: `GET super-admin` with `@Roles('SUPER_ADMIN')` returning `{ ok: true, scope: 'super-admin' }`, and `GET content` with `@Roles('SUPER_ADMIN', 'CONTENT')` returning `{ ok: true, scope: 'content' }`. Add a file header comment: these endpoints exist to prove AUTH-04 end to end and are the RBAC pattern every admin controller in Plans 04-05 and Phases 2-7 must copy — `@UseGuards(JwtAuthGuard, RolesGuard)` at the class level plus an explicit `@Roles(...)` on every route. Register the controller through a small `AdminProbeModule` (or directly in `AppModule` controllers) and confirm it is reachable under the global `api` prefix.

Run the full e2e auth spec and confirm all 13 cases now pass (this closes the RED opened in Task 1).
  </action>
  <verify>
    <automated>pnpm --filter api test -- roles.guard.spec.ts --watch=false &amp;&amp; pnpm --filter api run test:e2e -- auth.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter api test -- roles.guard.spec.ts` passes with at least 6 cases.
    - ALL 13 cases in `apps/api/test/auth.e2e-spec.ts` pass (Task 1's RED is now GREEN).
    - `apps/api/src/common/guards/roles.guard.ts` contains `getAllAndOverride` and `user?.type === 'STAFF'`.
    - `apps/api/src/modules/admin/admin-probe.controller.ts` contains `@UseGuards(JwtAuthGuard, RolesGuard)` and every route method carries an explicit `@Roles(` decorator (`grep -c "@Roles(" ` equals the number of `@Get(` occurrences).
    - With a WAREHOUSE bearer token: `curl -s -o /dev/null -w '%{http_code}' -H "Authorization: Bearer $T" localhost:4000/api/admin/_probe/super-admin` returns `403`.
    - With no token, the same URL returns `401`.
    - With a SUPER_ADMIN token, it returns `200`.
  </acceptance_criteria>
  <done>Staff RBAC is enforced at the API layer with deny-by-default semantics, unit-tested per-branch and proven end to end with 401/403/200 outcomes across four staff roles and a non-staff principal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous internet → `/api/auth/*` | Untrusted credentials and tokens enter here; this is the platform's primary attack surface |
| Bearer token → `request.user` | The JWT payload becomes the authorization subject for every downstream guard |
| API process → `accounts` / `refresh_tokens` | Password hashes and session material at rest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Spoofing | Credential stuffing / brute force on `POST /api/auth/login` | mitigate | `@Throttle({ limit: 5, ttl: 60_000 })` applied specifically to `login` and `register`, tighter than the global 120/min. Progressive lockout deferred to a later hardening pass. |
| T-01-15 | Information Disclosure | User enumeration via login responses | mitigate | `validateCredentials` runs a `bcrypt.compare` against a constant dummy hash on the account-not-found path so timing matches, and the controller returns a byte-identical 401 body for unknown-email and wrong-password. Asserted in e2e case 9 and in Task 2 acceptance. |
| T-01-16 | Tampering | JWT algorithm confusion / `alg: none` | mitigate | `algorithm: 'HS256'` pinned on sign and `algorithms: ['HS256']` pinned on verify in `JwtStrategy`; secret loaded via `ConfigService.getOrThrow('JWT_ACCESS_SECRET')` with a zod `min(32)` boot check, never a literal. |
| T-01-17 | Spoofing | Stolen refresh token replayed indefinitely | mitigate | Refresh tokens are single-use: `rotateRefreshToken` revokes the presented token and issues a new one inside one `$transaction`. Replay of a rotated token returns 401 (e2e case 7). `logout` revokes all outstanding tokens for the account (e2e case 8). |
| T-01-18 | Information Disclosure | Session material readable from a database dump | mitigate | Only `sha256(rawToken)` is persisted in `refresh_tokens.tokenHash`; `token.service.spec.ts` asserts a lookup by the raw value finds nothing. Passwords are `bcrypt` at cost ≥ 12. |
| T-01-19 | Elevation of Privilege | Under-privileged staff or a non-staff principal reaching admin routes | mitigate | `RolesGuard` is deny-by-default on any route carrying `@Roles(...)`: false for missing user, for `type !== 'STAFF'`, and for a role outside the allow-list. Proven with 401/403/200 across WAREHOUSE, CONTENT, SUPER_ADMIN and a RETAIL_CUSTOMER token. |
| T-01-20 | Elevation of Privilege | Stale role in a long-lived token after a demotion | mitigate | Access TTL is 15 minutes and `rotateRefreshToken` re-reads the `Account` before re-issuing, so a role/approval change takes effect on the next refresh rather than at token expiry only. |
| T-01-21 | Information Disclosure | `passwordHash` leaking through a response or Swagger example | mitigate | All controller responses use an explicit safe projection `{ id, email, type, role?, approvalStatus?, priceTierId? }`; acceptance asserts `passwordHash` appears nowhere in `auth.controller.ts` and nowhere in the register response body. |
| T-01-22 | Tampering | Mass assignment on register/login DTOs | mitigate | Global `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true })` from Plan 02 plus explicit `class-validator` constraints; `RegisterCustomerDto` cannot set `type` or `role`, and `registerCustomer` hardcodes `RETAIL_CUSTOMER`. |
</threat_model>

<verification>
1. `pnpm --filter api test` (unit) passes, including `token.service.spec.ts` and `roles.guard.spec.ts`.
2. `pnpm --filter api run test:e2e -- auth.e2e-spec.ts` passes all 13 cases.
3. Manual: log in as `admin@kidtoy.local`, call `/api/auth/me`, refresh, call `/api/auth/me` again with the new token — both 200; replay the old refresh token — 401.
4. `pnpm turbo run typecheck` exits 0.
5. `grep -rn --include=*.ts -E "secret\s*[:=]\s*['\"][A-Za-z0-9+/=]{16,}" apps/api/src` returns nothing.
</verification>

<success_criteria>
- One `AuthModule` over one `Account` table serves staff and retail customers (and is ready for business accounts in Plan 04).
- AUTH-01: staff log in and their session survives a refresh through rotating, single-use, hashed refresh tokens.
- AUTH-02: retail customers register and log in; duplicate emails 409, weak passwords 400.
- AUTH-04: staff RBAC is deny-by-default and enforced server-side, proven with 401/403/200 across all four staff roles plus a non-staff principal.
- No secret literal, no `passwordHash` leak, no user enumeration, no unbounded login attempts.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-bilingual-catalog-pricing/01-03-SUMMARY.md` when done.
</output>
